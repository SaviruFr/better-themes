[build]
  command = "pnpm --filter better-themes build && pnpm --filter vite build"
  publish = "examples/vite/dist"

# SPA routing - redirect all requests to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
